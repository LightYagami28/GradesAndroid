# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# Specifies the JVM arguments used for the daemon process.
# Increase the maximum heap size to utilize more RAM.
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8 -XX:+UseG1GC

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects.
org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class to reduce the size of the R class for that library
android.nonTransitiveRClass=true

# Optional: Enable Gradle's configuration on demand to improve build performance
org.gradle.configureondemand=true

# Optional: Enable the Gradle build cache to speed up builds
org.gradle.caching=true

# Optional: Set the Android Gradle Plugin version
# Update this to the latest stable version compatible with your Gradle version
android.gradle.plugin.version=8.0.0
